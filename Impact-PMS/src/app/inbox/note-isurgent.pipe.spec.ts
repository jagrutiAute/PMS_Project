import { NoteIsurgentPipe } from './note-isurgent.pipe';

describe('NoteIsurgentPipe', () => {
  it('create an instance', () => {
    const pipe = new NoteIsurgentPipe();
    expect(pipe).toBeTruthy();
  });
});
